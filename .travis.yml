# programming language
language: node_js

# cache dependencies
cache:
  directories:
    - ./pwa/node_modules

# log some version information
before_install:
  - node --version
  - npm --version
  - ls -la

# install dependencies
install:
  - cd pwa
  - npm i

# build the pwa
script:
  - cd pwa
  - npm run build --prod